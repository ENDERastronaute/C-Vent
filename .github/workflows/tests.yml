name: tests.yml
on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        compiler: [gcc, clang]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        uses: sudo apt update && sudo apt install -y cmake g++ clang

      - name: Configure CMake
        uses: |
          mkdir build
          cd build
          cmake .. -DCMAKE_CXX_COMPILER=${{ matrix.compiler }}

      - name: Build
        uses: |
          cd build
          cmake --build .

      - name: Run tests
        uses: |
          cd build
          ctest --output-on-failure
